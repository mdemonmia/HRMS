# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: UserLoginHistory.spec.js >> test user login history >> check user type filter with valid data @regression
- Location: tests\UserLoginHistory.spec.js:29:9

# Error details

```
ReferenceError: UserLoginHistoryPage is not defined
```

# Test source

```ts
  1  | import { test,expect } from "@playwright/test";
  2  | import { LoginPage } from "../pages/LoginPage";
  3  | import { UserMgtPage } from "../pages/User";
  4  | import { testData } from "../fixtures/testData";
  5  | 
  6  | const data = new testData();
  7  | 
  8  | test.describe('test user login history',()=>{
  9  |     test.beforeEach(async({page})=>{
  10 |         const login = new LoginPage(page);
  11 |         await login.goto(data.url);
  12 |         await login.Login(
  13 |             data.user_login.valid_login.username,
  14 |             data.user_login.valid_login.password
  15 |         )
  16 |         await login.clickMenuLink();
  17 | 
  18 |         const user =new UserMgtPage(page);
  19 |         await user.getUserMgtlink();
  20 | 
> 21 |         const userlogin = new UserLoginHistoryPage(page);
     |                           ^ ReferenceError: UserLoginHistoryPage is not defined
  22 |         await userlogin.getUserLoginHistory();
  23 |     })
  24 | 
  25 |     test.afterEach(async({page})=>{
  26 |         await page.close();
  27 |     })
  28 | 
  29 |     test('check user type filter with valid data @regression',async({page})=>{
  30 |         const userlogin = new UserLoginHistoryPage(page);
  31 |         await userlogin.getUserType(
  32 |             data.UserLoginHis.UserTypeName.userType
  33 |         );
  34 |         const tcount = await userlogin.getTotalcount();
  35 |         await expect(userlogin.tcount).toHaveText('Total : 20');
  36 |     })
  37 | 
  38 | })
```